<Window x:Class="AutoDownloader.UI.PreferencesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AutoDownloader Preferences - v1.8" Height="495" Width="695"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FontFamily="Segoe UI"
        Background="#FF1E1E1E">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF3A3A3A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="5" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="5" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF0099FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel Margin="15">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Save Settings" 
                    Style="{StaticResource ActionButtonStyle}"
                    Click="SaveButton_Click"
                    Background="#FF28A745" Height="30" Width="105" />
            <Button Content="Cancel" 
                    Style="{StaticResource ActionButtonStyle}"
                    Click="CancelButton_Click"
                    Background="#FF6C757D" Height="31" Width="64" />
        </StackPanel>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Label Content="API Key Configuration" Foreground="#FF007ACC" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

                <Label Content="TMDB API Key (Required for Naming/Verification):" Foreground="White"/>
                <TextBox x:Name="TmdbKeyTextBox" Height="30" Margin="0,0,0,10" 
                         ToolTip="Required for official show names and episode counts."/>

                <Label Content="TVDB API Key (Optional Fallback):" Foreground="White"/>
                <TextBox x:Name="TvdbKeyTextBox" Height="30" Margin="0,0,0,20"
                         ToolTip="Used as a secondary metadata source for better coverage on Anime/Plex."/>

                <Label Content="Gemini API Key (Optional for Smart Search):" Foreground="White"/>
                <TextBox x:Name="GeminiKeyTextBox" Height="30" Margin="0,0,0,20"
                         ToolTip="Used to search the web for download links based on show titles."/>

                <Label Content="Default Download Settings" Foreground="#FF007ACC" FontWeight="Bold" FontSize="16" Margin="0,10,0,10"/>

                <Label Content="Default Output Folder:" Foreground="White"/>
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="OutputFolderTextBox" Grid.Column="0" IsReadOnly="True" Height="30"/>
                    <Button Content="Browse..." Grid.Column="1" Click="BrowseButton_Click" Margin="5,0,0,0" 
                            Style="{StaticResource ActionButtonStyle}" Background="#FF4A4A4A"/>
                </Grid>

                <Label Content="Preferred Video Quality (yt-dlp format):" Foreground="White"/>
                <TextBox x:Name="QualityTextBox" Height="30" Margin="0,0,0,10" 
                         ToolTip="Example: bestvideo+bestaudio/best. (Advanced setting)"/>

            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Window>