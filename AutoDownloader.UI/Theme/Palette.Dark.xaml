<!--
    Dark - the original look.

    Palette only. The control styles live in Controls.xaml and reach these by
    DynamicResource, so swapping this one file changes the whole application at runtime.
    Every palette must define the same keys or the swap leaves holes.

    The system brush overrides are the important half: popups, menus and tooltips are drawn
    by Windows' default templates using those keys, so without them a dropdown opens as a
    sheet of the system colour regardless of what the styles say.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <SolidColorBrush x:Key="Surface"        Color="#FF1E1E1E"/>
    <SolidColorBrush x:Key="SurfaceRaised"  Color="#FF2D2D2D"/>
    <SolidColorBrush x:Key="SurfaceInput"   Color="#FF3A3A3A"/>
    <SolidColorBrush x:Key="SurfaceSunken"  Color="#FF0B0B0B"/>
    <SolidColorBrush x:Key="TextPrimary"    Color="#FFF0F0F0"/>
    <SolidColorBrush x:Key="TextMuted"      Color="#FFB0B0B0"/>
    <SolidColorBrush x:Key="TextDisabled"   Color="#FF808080"/>
    <SolidColorBrush x:Key="Accent"         Color="#FF007ACC"/>
    <SolidColorBrush x:Key="AccentText"     Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="BorderSubtle"   Color="#FF555555"/>

    <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}"        Color="#FF2D2D2D"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}"    Color="#FFF0F0F0"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}"       Color="#FF2D2D2D"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}"   Color="#FFF0F0F0"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlLightBrushKey}"  Color="#FF3A3A3A"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlDarkBrushKey}"   Color="#FF555555"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuBrushKey}"          Color="#FF2D2D2D"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuTextBrushKey}"      Color="#FFF0F0F0"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuBarBrushKey}"       Color="#FF2D2D2D"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.InfoBrushKey}"          Color="#FF2D2D2D"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.InfoTextBrushKey}"      Color="#FFF0F0F0"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.GrayTextBrushKey}"      Color="#FF808080"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"     Color="#FF007ACC"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFFFFFFF"/>

    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}"
                     Color="#FF3A3A3A"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}"
                     Color="#FFF0F0F0"/>

</ResourceDictionary>
