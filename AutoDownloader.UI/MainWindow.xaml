<Window x:Class="AutoDownloader.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoDownloader.UI"
        mc:Ignorable="d"
        Title="Auto Downloader v1.1" Height="600" Width="800" MinHeight="450" MinWidth="650"
        FontFamily="Segoe UI"
        Background="#f9f9f9">

    <!-- This section is like a mini-stylesheet for our window -->
    <Window.Resources>
        <!-- Style for all TextBoxes -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#ccc"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="5" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for our main "Start Download" button -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#007aff"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10 8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="5" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <!-- Add a hover effect -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0056b3"/>
                </Trigger>
                <!-- Add a pressed effect -->
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#004a99"/>
                </Trigger>
                <!-- Style for when the button is disabled -->
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#aaa"/>
                    <Setter Property="Foreground" Value="#eee"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style for the "Browse" button -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#e0e0e0"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Padding" Value="10 8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="5" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#c7c7c7"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#b0b0b0"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#aaa"/>
                    <Setter Property="Foreground" Value="#eee"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid>
        <!-- Main layout is a DockPanel -->
        <DockPanel>

            <!-- 1. Status Bar at the bottom -->
            <StatusBar DockPanel.Dock="Bottom" Background="#f0f0f0" BorderThickness="0 1 0 0" BorderBrush="#ddd">
                <StatusBarItem>
                    <TextBlock x:Name="StatusTextBlock" Text="Ready." Padding="5"/>
                </StatusBarItem>
            </StatusBar>

            <!-- 2. Control Panel at the top -->
            <Border DockPanel.Dock="Top" Padding="15" BorderBrush="#ddd" BorderThickness="0 0 0 1" Background="White">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Row 0: URL/Search Term Input -->
                    <Label Grid.Row="0" Grid.Column="0" Content="URL or Search Term (TV Show, Movie, Anime):" FontWeight="Bold" Foreground="#333"/>

                    <TextBox x:Name="UrlTextBox" 
                             Grid.Row="1" Grid.Column="0" 
                             Margin="0 5 10 0" 
                             KeyDown="UrlTextBox_KeyDown"/>
                    <!-- Style is applied automatically -->

                    <Button x:Name="StartDownloadButton" 
                            Grid.Row="1" Grid.Column="1" 
                            Content="  Download  " 
                            Margin="0 5 0 0"
                            Style="{StaticResource PrimaryButton}"
                            Click="StartDownloadButton_Click"/>

                    <!-- Row 2: Output Folder -->
                    <Label Grid.Row="2" Grid.Column="0" Content="Output Folder:" Margin="0 15 0 0" FontWeight="Bold" Foreground="#333"/>
                    <Grid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0 5 0 0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="OutputFolderTextBox" 
                                 Grid.Column="0" 
                                 Margin="0 0 10 0" 
                                 IsReadOnly="True" 
                                 Background="#eee"/>
                        <Button x:Name="BrowseButton" 
                                Grid.Column="1" 
                                Content="  Browse...  " 
                                Style="{StaticResource SecondaryButton}"
                                Click="BrowseButton_Click"/>
                    </Grid>
                </Grid>
            </Border>

            <!-- 3. Log Window fills the rest of the space -->
            <TextBox x:Name="OutputLogTextBox" 
                     FontFamily="Consolas" 
                     FontSize="13"
                     IsReadOnly="True" 
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Background="#1e1e1e" 
                     Foreground="#d4d4d4"
                     BorderThickness="0"
                     Padding="10"/>

        </DockPanel>
    </Grid>
</Window>

